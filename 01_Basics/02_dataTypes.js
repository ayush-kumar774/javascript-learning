"use strict"; // treat all JS code as newer version

// alert (3 + 3) // we are using nodejs, not browser

let name = "Aayush";
let age = 21;
let isLoggedIn = false;
let state;
let nullTest = null;

// number => 
// bigint
// string => ""
// boolean => true/false
// null => standalone value (It is an object)
// undefined => value is not assigned yet.
// symbol => unique

// object

console.table([typeof(name), typeof(age), typeof(isLoggedIn), typeof(state), typeof(nullTest)]);

